- correctness-1 [correctness] I4/E1/R1 :: The storage-nearly-full repair reads two keys hub_readings never emits, so it can never fire
    why: hub_readings produces storage_total_gb / storage_free_gb / storage_used_percent; _storage asks for storage_total and storage_free, gets None for…
    cite: custom_components/tapo_h500/repairs.py:114-118 — total = coordinator.readings.get("storage_total") free =…
- offline-1 [offline-resilience] I4/E1/R1 :: The hub_unreachable repair issue can only fire while the hub IS reachable — it is raised from inside the success path
    why: repairs.async_check has exactly one call site, at the end of _poll(), so it runs only when the poll completed. When the hub is actually down _poll()…
    cite: custom_components/tapo_h500/coordinator.py:896-898 (the only call site, at the end of _poll): try: from…
- offline-3 [offline-resilience] I4/E1/R2 :: Media-session credentials are snapshotted once at connect() and never refreshed, while the control channel is documented to silently re-login across hub reboots
    why: connect() copies superSecretKey and the encryption method off the pytapo object, and connect() is called exactly once per config-entry lifetime (only…
    cite: custom_components/tapo_h500/api.py:180-181 (captured once, uncommented, unlike the rest of this file):…
- frontend-cards-2 [frontend-cards] I4/E1/R2 :: The 60-second poll rebuilds innerHTML wholesale, restarting any open <video> from zero with autoplay
    why: `_load()` always calls `_render()` in its finally, and `_render()` assigns `this._card.innerHTML`, destroying and recreating every child.…
    cite: custom_components/tapo_h500/www/tapo-h500-card.js:441 — `this._timer = setInterval(() => this._load(),…
- docs-1 [docs] I4/E2/R1 :: reference.md and protocol-notes.md still say doorbell presses are unidentified and RING_ALARM_TYPES is empty
    why: Two of the three docs files declare the ring code unsolved and tell the reader to go edit const.py themselves, while const.py has shipped…
    cite: docs/reference.md:86-88 `**Doorbell presses are not distinguishable yet, but finding the code is now a` /…
- docs-2 [docs] I4/E2/R1 :: README and reference.md quote translation keys as entity IDs for four entities that actually get name-derived IDs
    why: Entities set `_attr_has_entity_name = True`, so Home Assistant slugifies the *display name*, not the translation key, into the entity_id. Four…
    cite: README.md:29 `- **Turn the auto-upgrade switch off** (\`switch.<hub>_auto_upgrade\`) — it` but…
- correctness-2 [correctness] I4/E2/R2 :: Writing a hub setting refreshes the coordinator but not readings, so every control snaps back to its old value for up to a minute
    why: H500HubControl.apply promises "one poll after the write ... so the new value is not guessed at", but the poll it triggers only fetches hub_status…
    cite: custom_components/tapo_h500/hub_control.py:38-40 — # One poll after the write, not one per call: this hub is…
- tests-1 [tests] I3/E1/R1 :: SAFE_CAMERA has no key-existence test, and two of its six keys are dead — `ai_enhance_enabled` (wrong case) and `battery_percent` (proven not to exist)
    why: The author already wrote exactly this guard for SAFE_READINGS after six of sixteen keys turned out wrong, but never extended it to SAFE_CAMERA — the…
    cite: custom_components/tapo_h500/diagnostics.py:53-56 allow-lists keys read straight off the raw hub record: ```…
- security-2 [security] I4/E2/R2 :: The preview view accepts any start_time, opening an unbounded media session per request and 500ing with a traceback on out-of-range values
    why: H500PreviewView is the integration's only HTTP endpoint and its only input validation is a sign check. Any authenticated Home Assistant user — or…
    cite: custom_components/tapo_h500/preview.py:51-56 — the entire validation: try: index, start = int(camera_index),…
- perf-2 [performance] I3/E1/R1 :: attach_detections falls back to an O(clips x detections) scan on every poll
    why: When a clip's start second is not an exact key in by_time, the code scans the whole detections dict to find one within MATCH_SECONDS. One clip off by…
    cite: clips.py:223-225 `match = by_time.get(moment) or next(\n (found for when, found in by_time.items()\n if…
- frontend-cards-1 [frontend-cards] I4/E2/R2 :: The "Recording…" pulse mostly never appears, and when it does it lingers up to 45s after the event ended
    why: `set hass` stores the new state map but only renders on the very first call, so the only periodic render is the 60s poll. `live` is computed at…
    cite: custom_components/tapo_h500/www/tapo-h500-card.js:432-438 — `set hass(hass) {\n this._hass = hass;\n if…
- correctness-4 [correctness] I3/E1/R1 :: Every preview thumbnail re-fetches the whole paired-camera list from the hub instead of using the cached one
    why: The preview view already has the coordinator in hand, and the coordinator caches self.cameras precisely because the call is expensive; its own…
    cite: custom_components/tapo_h500/preview.py:61-63 — try: camera = await hass.async_add_executor_job(…
- offline-2 [offline-resilience] I4/E2/R2 :: Auto-restart cooldown and circuit breaker live only in coordinator memory, so every Home Assistant restart re-arms an immediate hub reboot
    why: _auto_restarted starts at 0.0 and _auto_restart_broken at False in __init__, and there is no Store or hass.data persistence anywhere in the…
    cite: custom_components/tapo_h500/coordinator.py:141-142: self._auto_restarted = 0.0 self._auto_restart_broken =…
- frontend-cards-3 [frontend-cards] I3/E1/R1 :: A clicked button is disabled and only a re-render re-enables it — cancelling the name prompt leaves it dead for up to 60 seconds
    why: `_onClick` disables the button up front and relies on the subsequent `_render()` to replace it with a fresh enabled one. The Cancel branch `return`s…
    cite: custom_components/tapo_h500/www/tapo-h500-card.js:507-512 — `const button =…
- tests-4 [tests] I3/E2/R1 :: preview.py's H500PreviewView.get — a registered HTTP endpoint with five error branches — has no test at all
    why: This is the view every recordings-card thumbnail hits. `async_preview_clip` behind it is thoroughly tested in test_media_session.py, but the request…
    cite: No test references `H500PreviewView` or `PREVIEW_PATH`; the only tests/ hit for `preview_url` is a string…
- ha-conformance-1 [ha-conformance] I4/E3/R2 :: Bad credentials retry the hub login forever: no ConfigEntryAuthFailed, no reauth flow
    why: async_setup_entry turns every connect() failure into ConfigEntryNotReady, so a permanent auth failure (changed camera-account or cloud password)…
    cite: custom_components/tapo_h500/__init__.py:197-202 — `try:` / `await…
- ha-conformance-2 [ha-conformance] I3/E2/R1 :: strings.json has rotted 102 keys behind translations/en.json, and verify.sh only checks entity keys
    why: strings.json holds 69 flattened keys against en.json's 165: it is missing every `issues.*` entry (all nine repair issues plus the unnamed_face…
    cite: tools/verify.sh:46-51 — "# Every entity translation key an entity declares must exist, or the frontend / #…
- security-1 [security] I4/E3/R2 :: A rejected login is retried forever as ConfigEntryNotReady instead of raising ConfigEntryAuthFailed
    why: async_setup_entry funnels every connect() failure into ConfigEntryNotReady, which Home Assistant retries on a backoff for as long as it runs, and…
    cite: custom_components/tapo_h500/__init__.py:196-201: try: await hass.async_add_executor_job(client.connect)…
- dx-3 [dx] I3/E2/R1 :: No CI at all — the only gate is a local script a human must remember to run, on a repo HACS installs from
    why: `tools/verify.sh` is a good gate that nothing enforces. The GitHub remote is the one HACS users install from, so a push that skips the script ships…
    cite: `find . -name .github -not -path './.venv/*'` returns nothing — no workflow exists. The publishable remote is…
- tests-5 [tests] I3/E2/R1 :: The Lovelace card's error state is never exercised — `_load()`'s catch and the `<div class="error">` render in H500Base are untested for all eight cards
    why: Every card inherits this path, and it is what a user sees whenever the WebSocket call or the list_recordings service fails. No test in the 78-test…
    cite: custom_components/tapo_h500/www/tapo-h500-card.js:499-500 sets the state: ``` } catch (err) { this._error =…
- perf-1 [performance] I4/E3/R3 :: faces_seen()/people() rescan every clip on every camera 20+ times per 2-second poll
    why: faces_seen() is a full pass over every clip of every camera (face_ids parse, trail build, per-face sort, direction+prowling), and nothing caches it.…
    cite: coordinator.py:296 `for face_id, face in self.faces_seen(clips=clips).items():` (inside people());…
- dx-1 [dx] I4/E4/R2 :: 18 of 31 component modules (~4,000 LOC) are never imported by any test — ast.parse is their only automated check
    why: verify.sh's only check for these modules is a syntax parse, and their tests assert on file text rather than behaviour. A NameError, a stale `from…
    cite: tools/verify.sh:33-34 is the entire check for these files: ` ast.parse(path.read_text(), str(path))` /…