- correctness-1 [correctness] I4/E1/R1 :: The storage-nearly-full repair reads two keys hub_readings never emits, so it can never fire
    why: repairs._storage asks coordinator.readings for `storage_total` and `storage_free`; hub_readings only ever produces `storage_total_gb`,…
    cite: Verified at HEAD b764e98, line numbers unchanged (iteration 1 did not touch repairs.py or status.py).…
- correctness-i2-1 [correctness] I5/E2/R2 :: A failed automatic download is never retried, so the hub's own documented media outage permanently loses that footage
    why: _download_new marks every clip seen in _seen_clips BEFORE spawning the download, and only one of the three failure branches un-marks it.…
    cite: custom_components/tapo_h500/coordinator.py:967 -- clips are marked seen before any download is…
- frontend-cards-2 [frontend-cards] I4/E1/R2 :: The 60-second poll rebuilds innerHTML wholesale, restarting any open <video> from zero with autoplay
    why: _load() always calls _render() in its finally, and _render() assigns this._card.innerHTML, destroying and recreating every child element.…
    cite: Verified at HEAD b764e98. www/tapo-h500-card.js was not touched by iteration 1, so line numbers are…
- correctness-i2-2 [correctness] I4/E2/R1 :: Card and notification downloads write no usable sidecar, so every manually saved clip is invisible to the by-type browser and to find_face -- permanently
    why: The sidecar JSON is the only durable record of what a clip was and who was in it once the hub's one-day index rolls over; both the media…
    cite: custom_components/tapo_h500/__init__.py:458-460 -- no faces=, and detected is empty whenever the caller…
- offline-1 [offline-resilience] I4/E2/R2 :: The hub_unreachable repair issue can only fire while the hub IS reachable -- it is raised from inside the success path
    why: repairs.async_check has exactly one call site, near the end of _poll(), after `self._polls += 1`. When the hub is down, _poll() raises…
    cite: Verified at HEAD b764e98. Line numbers shifted by one from the iteration-1 board. Only call site,…
- correctness-4 [correctness] I3/E1/R1 :: Every preview thumbnail re-fetches the whole paired-camera list from the hub instead of using the cached one
    why: The preview view already holds the coordinator, and the coordinator caches self.cameras precisely because the call is expensive -- its own…
    cite: Verified at HEAD b764e98. api.py line numbers shifted by iteration 1 (248 -> 331); preview.py numbers…
- perf-2 [performance] I3/E1/R1 :: attach_detections falls back to an O(clips x detections) scan on every poll
    why: When a clip's start second is not an exact key in by_time, the code scans the whole detections dict looking for one within MATCH_SECONDS. One…
    cite: Verified at HEAD b764e98, line numbers unchanged. custom_components/tapo_h500/clips.py:223-225 -- match…
- docs-1 [docs] I3/E1/R1 :: reference.md and protocol-notes.md still say doorbell presses are unidentified and RING_ALARM_TYPES is empty
    why: const.py has shipped `RING_ALARM_TYPES: set[int] = {17}` and both limitations.md and one section of protocol-notes.md document the press as…
    cite: Verified at HEAD b764e98. Two reference.md citations shifted by the board's count; the real lines are…
- docs-2 [docs] I3/E1/R1 :: README and reference.md quote translation keys as entity IDs for four entities that actually get name-derived IDs
    why: Entities set _attr_has_entity_name = True, so Home Assistant slugifies the display name, not the translation key, into the entity_id. Three…
    cite: Verified at HEAD b764e98; the reference.md sibling-switch citation was line 37, not 36. README.md:29 --…
- offline-7 [offline-resilience] I4/E2/R2 :: A failed automatic download is never retried, so every clip recorded during a wedge or an outage is lost permanently even though the hub still holds it for 24 hours
    why: _fresh marks a clip's start time in _seen_clips the moment it is considered, and _download's two failure paths (EmptyRecordingError and…
    cite: custom_components/tapo_h500/coordinator.py:1100-1112 — both failure paths return with the clip still…
- offline-8 [offline-resilience] I4/E2/R2 :: The media lock is held across an unbounded session handshake, and both wedge probes are gated off exactly while it is held, so one stalled session blinds the detector and freezes the media path until a reload
    why: iter_recording takes the client's single asyncio.Lock with no timeout, then enters the session context manager — the TCP connect, digest…
    cite: custom_components/tapo_h500/api.py:602 — no timeout on acquisition: async with self._lock:…
- offline-9 [offline-resilience] I3/E1/R1 :: The camera-list refresh is gated on a poll counter that only advances on a successful poll, so the list can never be re-read while polls are failing
    why: _polls += 1 sits at the very end of _poll, after the per-camera activity fetch that raises UpdateFailed. The camera-list refresh at the top of…
    cite: The gate, custom_components/tapo_h500/coordinator.py:704 — if not self.cameras or self._polls %…
- frontend-cards-7 [frontend-cards] I4/E2/R2 :: _load() has no request/response correlation, so a camera or day change either does nothing or lands the previous selection's clips under the new label
    why: Two separate holes in one path. First, _load() early-returns while _busy, so the reload that the camera picker and the day pager queue up right…
    cite: custom_components/tapo_h500/www/tapo-h500-card.js:481-497 — async _load() { if (!this._hass ||…
- preview-500 [correctness] I3/E1/R1 :: preview.py:67 sits outside every try, so an out-of-range start_time returns a 500 with a traceback
    why: Surfaced by the iteration-3 premise check that disqualified security-2 — the disqualification was of the ARGUMENT (a session-churn/traversal…
    cite: Iteration-3 premise verifier, verdict FALSE on the claim that no 500 is user-visible: "preview.py:67…
- dx-3 [dx] I3/E2/R1 :: No CI at all -- the only gate is a local script a human must remember to run, on a repo HACS installs from
    why: tools/verify.sh is a good gate that nothing enforces. The GitHub remote is the one HACS users install from, so a push that skips the script…
    cite: Verified at HEAD b764e98. `find . -name .github -not -path './.venv/*'` returns nothing, and a top-level…
- offline-3 [offline-resilience] I3/E1/R2 :: Media-session credentials are snapshotted once at connect() and never refreshed, while the control channel is documented to silently re-login across hub reboots
    why: connect() copies superSecretKey and the encryption method off the pytapo object and is called exactly once per config-entry lifetime, while the…
    cite: Verified at HEAD b764e98. api.py line numbers shifted substantially in iteration 1 (180-181 -> 263-264,…
- correctness-i2-3 [correctness] I4/E3/R2 :: "Keep the newest N doorbell presses however old they get" only protects clips still inside the hub's 24-hour index
    why: _protected builds the keep-set from self.clips_for(index), which is the polled window -- _poll asks the hub for `now - LOOKBACK_SECONDS` and…
    cite: custom_components/tapo_h500/coordinator.py:1063-1077 -- the promise, and the 24h-only source it is built…
- correctness-i2-4 [correctness] I3/E2/R1 :: The contact-sheet image never goes unavailable and never re-stamps at local midnight, so it serves a 500 every quiet morning
    why: H500ContactSheet.async_image builds the sheet for today's LOCAL date and returns None when that folder holds no thumbnails, but the class…
    cite: custom_components/tapo_h500/image.py:104-105 -- the claim: Unavailable on a quiet day rather than…